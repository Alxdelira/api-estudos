{
  "version": 2,
  "builds": [
    {
      "src": "server.js", 
      "use": "@vercel/node",
      "config": {
        "build": {
          "env": {
            "CI": "true"
          },
          "scripts": {
            "postinstall": "npm run install-swagger"
          }
        }
      }
    }
  ],
  "routes": [{ "src": "(.*)", "dest": "server.js" }]
}
